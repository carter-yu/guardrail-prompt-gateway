[project]
name = "guardrail-prompt-gateway"
version = "0.4.0"
description = "Learning demo: deterministic prompt gates around a pinned LLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "jinja2>=3.1",
    "langgraph>=1.2.11",
    "pydantic>=2.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0",
    "structlog",
    "uvicorn>=0.32",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "src/guardrail_prompt_gateway",
    "src/lib",
    "src/schemas",
    "src/services",
    "src/app",
    "src/graph",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
src = ["src", "tests"]
